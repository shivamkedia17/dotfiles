# check if current shell is zsh
# if not,
	# check if zsh is installed as a shell
		# if not, install zsh
	# set zsh as the current shell


# clone dotfiles setup using dotfiles setup_script


# check if $ZSH = $HOME/.zsh
# if not,
	# check if .zsh exists with plugins/ and themes/
		# if not, create plugin and themes
	# set $ZSH

# install the necessary git submodules (plugins and themes)

